import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Sikora.Sonya',
  appName: 'Sonya',
  webDir: 'dist/sonya',
  bundledWebRuntime: false
};

export default config;
